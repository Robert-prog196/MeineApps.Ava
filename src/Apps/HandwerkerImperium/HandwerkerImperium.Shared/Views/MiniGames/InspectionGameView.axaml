<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:HandwerkerImperium.ViewModels"
             xmlns:mi="using:Material.Icons.Avalonia"
             xmlns:loc="using:MeineApps.Core.Ava.Localization"
             xmlns:skia="using:Avalonia.Labs.Controls"
             x:Class="HandwerkerImperium.Views.MiniGames.InspectionGameView"
             x:DataType="vm:InspectionGameViewModel">

  <Grid RowDefinitions="Auto,Auto,*,Auto,Auto" Margin="10,20,10,20">

    <!-- ================================================================= -->
    <!-- ROW 0: HEADER                                                     -->
    <!-- ================================================================= -->
    <Border Grid.Row="0"
            Classes="CardElevated"
            Padding="16,12">
      <Grid ColumnDefinitions="Auto,*,Auto">
        <!-- Cancel Button -->
        <Button Classes="Text"
                Command="{Binding CancelCommand}">
          <mi:MaterialIcon Kind="Close" Width="24" Height="24" />
        </Button>

        <!-- Titel -->
        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
          <StackPanel Orientation="Horizontal"
                      HorizontalAlignment="Center"
                      Spacing="6">
            <mi:MaterialIcon Kind="ClipboardCheck" Width="22" Height="22" />
            <TextBlock Text="{loc:Translate InspectionGame}"
                       FontSize="18" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
          </StackPanel>
          <TextBlock Text="{Binding DifficultyStars}"
                     HorizontalAlignment="Center"
                     FontSize="12"
                     Foreground="{DynamicResource TextSecondaryBrush}" />
          <TextBlock Text="{Binding TaskProgressDisplay}"
                     HorizontalAlignment="Center"
                     FontSize="11" FontWeight="SemiBold"
                     Foreground="{StaticResource CraftPrimaryBrush}"
                     IsVisible="{Binding TaskProgressDisplay, Converter={x:Static StringConverters.IsNotNullOrEmpty}}" />
        </StackPanel>

        <!-- Timer -->
        <Border Grid.Column="2"
                Background="{DynamicResource SurfaceBrush}"
                CornerRadius="8"
                Padding="12,6">
          <StackPanel Orientation="Horizontal" Spacing="4">
            <mi:MaterialIcon Kind="Clock" Width="16" Height="16" />
            <TextBlock Text="{Binding TimeRemaining, StringFormat='{}{0}s'}"
                       FontSize="14" FontWeight="Bold"
                       Foreground="{DynamicResource TextPrimaryBrush}" />
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <!-- ================================================================= -->
    <!-- ROW 1: STATS (Fortschrittsbalken + Fehlalarme)                    -->
    <!-- ================================================================= -->
    <StackPanel Grid.Row="1" Spacing="6" Margin="0,8,0,0">

      <!-- Anleitung (vor Spielstart) -->
      <TextBlock Text="{loc:Translate InspectionInstruction}"
                 FontSize="12"
                 Foreground="{DynamicResource TextSecondaryBrush}"
                 HorizontalAlignment="Center"
                 TextWrapping="Wrap"
                 TextAlignment="Center"
                 IsVisible="{Binding !IsPlaying}" />

      <!-- Anleitung (waehrend Spiel) -->
      <TextBlock Text="{loc:Translate TapToInspect}"
                 FontSize="12"
                 Foreground="{DynamicResource TextSecondaryBrush}"
                 HorizontalAlignment="Center"
                 IsVisible="{Binding IsPlaying}" />

      <!-- Fortschrittsbalken -->
      <Grid ColumnDefinitions="Auto,*,Auto"
            IsVisible="{Binding IsPlaying}"
            Margin="0,0,0,2">
        <mi:MaterialIcon Kind="MagnifyPlus" Width="20" Height="20" />
        <skia:SKCanvasView Grid.Column="1"
                           Height="16"
                           Margin="12,0"
                           PaintSurface="OnPaintInspectionProgress" />
        <TextBlock Grid.Column="2" FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   VerticalAlignment="Center">
          <Run Text="{Binding FoundDefects}" />
          <Run Text="/" />
          <Run Text="{Binding TotalDefects}" />
        </TextBlock>
      </Grid>

      <!-- Fehlalarme -->
      <StackPanel Orientation="Horizontal" Spacing="4"
                  HorizontalAlignment="Center"
                  IsVisible="{Binding IsPlaying}">
        <mi:MaterialIcon Kind="AlertCircle" Width="16" Height="16"
                         Foreground="{DynamicResource ErrorBrush}" />
        <TextBlock FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}">
          <Run Text="{loc:Translate FalseAlarmsLabel}" />
          <Run Text=" " />
          <Run Text="{Binding FalseAlarms}" />
        </TextBlock>
      </StackPanel>
    </StackPanel>

    <!-- ================================================================= -->
    <!-- ROW 2: SKIA GAME AREA                                            -->
    <!-- ================================================================= -->
    <Border Grid.Row="2" Background="{DynamicResource CardBrush}" CornerRadius="8" Margin="0,8" ClipToBounds="True">
      <skia:SKCanvasView x:Name="GameCanvas" MinHeight="240" />
    </Border>

    <!-- ================================================================= -->
    <!-- ROW 3: RESULT DISPLAY                                            -->
    <!-- ================================================================= -->
    <Border x:Name="ResultBorder" Grid.Row="3"
            Classes="Card StatsCard"
            IsVisible="{Binding IsResultShown}"
            Padding="24">
      <StackPanel Spacing="16" HorizontalAlignment="Center">
        <!-- Emoji -->
        <TextBlock Text="{Binding ResultEmoji}"
                   FontSize="48"
                   HorizontalAlignment="Center" />

        <!-- Sterne-Bewertung (staggered via Code-Behind) -->
        <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
          <mi:MaterialIcon x:Name="Star1Panel" Kind="Star" Width="32" Height="32"
                           Foreground="#FFD700"
                           Opacity="{Binding Star1Opacity}" />
          <mi:MaterialIcon x:Name="Star2Panel" Kind="Star" Width="32" Height="32"
                           Foreground="#FFD700"
                           Opacity="{Binding Star2Opacity}" />
          <mi:MaterialIcon x:Name="Star3Panel" Kind="Star" Width="32" Height="32"
                           Foreground="#FFD700"
                           Opacity="{Binding Star3Opacity}" />
        </StackPanel>

        <!-- Rating Text (Farbe wird per Code-Behind gesetzt) -->
        <TextBlock x:Name="RatingText"
                   Text="{Binding ResultText}"
                   FontSize="24" FontWeight="Bold"
                   Foreground="{DynamicResource TextPrimaryBrush}"
                   HorizontalAlignment="Center" />

        <!-- Belohnungen -->
        <StackPanel Orientation="Horizontal" Spacing="20"
                    HorizontalAlignment="Center">
          <StackPanel Orientation="Horizontal" Spacing="4">
            <mi:MaterialIcon Kind="CashMultiple" Width="20" Height="20"
                             Foreground="{DynamicResource SuccessBrush}" />
            <TextBlock x:Name="RewardMoneyText"
                       Text="{Binding RewardAmount, StringFormat='+{0:N0} &#x20AC;'}"
                       FontSize="14" FontWeight="Bold"
                       Foreground="{DynamicResource SuccessBrush}"
                       VerticalAlignment="Center" />
          </StackPanel>

          <StackPanel Orientation="Horizontal" Spacing="4">
            <mi:MaterialIcon Kind="Star" Width="20" Height="20"
                             Foreground="Gold" />
            <TextBlock x:Name="RewardXpText"
                       Text="{Binding XpAmount, StringFormat='+{0} XP'}"
                       FontSize="14" FontWeight="Bold"
                       Foreground="Gold"
                       VerticalAlignment="Center" />
          </StackPanel>
        </StackPanel>
      </StackPanel>
    </Border>

    <!-- Countdown Overlay (3, 2, 1, Los!) -->
    <Border x:Name="CountdownOverlay"
            Grid.RowSpan="5"
            Background="#CC000000"
            IsVisible="{Binding IsCountdownActive}">
      <TextBlock x:Name="CountdownTextBlock"
                 Text="{Binding CountdownText}"
                 FontSize="72" FontWeight="Bold"
                 Foreground="#FFD700"
                 HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Border>

    <!-- Tutorial Overlay (beim ersten Spielstart) -->
    <Border Grid.RowSpan="5" Background="#DD000000"
            IsVisible="{Binding ShowTutorial}">
      <Border Background="{DynamicResource CardBrush}"
              CornerRadius="16"
              Padding="24"
              Margin="32"
              MaxWidth="400"
              HorizontalAlignment="Center"
              VerticalAlignment="Center">
        <StackPanel Spacing="16">
          <TextBlock Text="{Binding TutorialTitle}"
                     FontSize="22" FontWeight="Bold"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     HorizontalAlignment="Center" />
          <TextBlock Text="{Binding TutorialText}"
                     FontSize="14"
                     Foreground="{DynamicResource TextSecondaryBrush}"
                     TextWrapping="Wrap"
                     TextAlignment="Center"
                     LineHeight="22" />
          <Button Content="{loc:Translate TutorialGotIt}"
                  Classes="Primary"
                  Command="{Binding DismissTutorialCommand}"
                  HorizontalAlignment="Center"
                  MinWidth="160"
                  Margin="0,8,0,0" />
        </StackPanel>
      </Border>
    </Border>

    <!-- ================================================================= -->
    <!-- ROW 4: ACTION BUTTONS                                            -->
    <!-- ================================================================= -->
    <StackPanel Grid.Row="4" Spacing="16">

      <!-- Start Button (versteckt waehrend Spiel und nach Ergebnis) -->
      <Panel IsVisible="{Binding !IsResultShown}">
        <Button Content="{loc:Translate StartInspection}"
                Classes="Primary"
                Command="{Binding StartGameCommand}"
                IsVisible="{Binding !IsPlaying}"
                HorizontalAlignment="Center"
                MinWidth="220" />
      </Panel>

      <!-- Letzte Aufgabe: Ad + Weiter -->
      <Grid ColumnDefinitions="*,*"
            ColumnSpacing="12">
        <Grid.IsVisible>
          <MultiBinding Converter="{x:Static BoolConverters.And}">
            <Binding Path="IsResultShown" />
            <Binding Path="IsLastTask" />
          </MultiBinding>
        </Grid.IsVisible>

        <Button Classes="Secondary"
                IsEnabled="{Binding CanWatchAd}"
                Command="{Binding WatchAdCommand}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center">
          <TextBlock Text="{loc:Translate WatchAdForDouble}"
                     TextTrimming="CharacterEllipsis" />
        </Button>

        <Button Grid.Column="1"
                Content="{Binding ContinueButtonText}"
                Classes="Primary"
                Command="{Binding ContinueCommand}"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Center" />
      </Grid>

      <!-- Zwischen-Aufgabe: Nur Weiter-Button -->
      <Button Content="{Binding ContinueButtonText}"
              Classes="Primary"
              Command="{Binding ContinueCommand}"
              HorizontalAlignment="Center"
              MinWidth="200">
        <Button.IsVisible>
          <MultiBinding Converter="{x:Static BoolConverters.And}">
            <Binding Path="IsResultShown" />
            <Binding Path="!IsLastTask" />
          </MultiBinding>
        </Button.IsVisible>
      </Button>

    </StackPanel>

  </Grid>

</UserControl>
