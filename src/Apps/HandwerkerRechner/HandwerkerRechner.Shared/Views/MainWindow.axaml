<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:HandwerkerRechner.ViewModels"
        xmlns:views="using:HandwerkerRechner.Views"
        xmlns:splash="using:MeineApps.UI.Controls"
        x:Class="HandwerkerRechner.Views.MainWindow"
        x:DataType="vm:MainViewModel"
        Title="HandwerkerRechner"
        Icon="avares://HandwerkerRechner.Shared/Assets/icon.png"
        Width="400"
        Height="750"
        MinWidth="360"
        MinHeight="500"
        Background="{DynamicResource BackgroundBrush}">

  <Panel AutomationProperties.AutomationId="MW_RootPanel">
    <views:MainView AutomationProperties.AutomationId="MW_MainView" />
    <splash:SplashOverlay AutomationProperties.AutomationId="MW_SplashOverlay"
                          AppName="HandwerkerRechner"
                          IconSource="avares://HandwerkerRechner.Shared/Assets/icon.png" />
  </Panel>

</Window>
